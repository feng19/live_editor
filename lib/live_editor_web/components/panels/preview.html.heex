<div id="artboard" phx-hook="Draggable">
  <div :for={{id, preview}<-@previews} id={id} draggable="true" class={"draggable #{preview.class}" <> if @select_id==id, do: " selected", else: "" }>
  <%= preview.rendered %>
  </div>
</div>
